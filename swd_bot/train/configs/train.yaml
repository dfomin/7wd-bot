epochs: 10
device: cpu
output_path: ../../models
defaults:
  - model: torch_baseline
  - data_provider@train_data_loader: torch_train
  - data_provider@valid_data_loader: torch_valid

  - _self_
  - override hydra/hydra_logging: disabled
  - override hydra/job_logging: disabled
hydra:
  output_subdir: null
  run:
    dir: .